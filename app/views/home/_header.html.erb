<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

        <%= link_to 'Star Wars Weather Forecast', root_path, class:"navbar-brand"%>


        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to "Planets", planets_path, class: "nav-link" %>
                </li>
            </ul>

            <form class="d-flex">

                <%= form_tag(root_path, method: 'get', class: 'navbar-form navbar-right') do %>
                <%= search_field_tag 'search', nil, class: 'form-control me-2', placeholder: 'Search'  %>
                <%= submit_tag "Submit", class: 'btn btn-outline-success', :name => nil %>
                <% end %>

            </form>
        </div>
    </div>
</nav>